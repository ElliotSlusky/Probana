{
  "name": "probana",
  "license": "UNLICENSED",
  "scripts": {
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ probana",
    "create-local": "graph create --node http://localhost:8020/ probana",
    "remove-local": "graph remove --node http://localhost:8020/ probana",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 probana",
    "test": "graph test"
  },
  "dependencies": { "@graphprotocol/graph-cli": "0.86.0" }
}
